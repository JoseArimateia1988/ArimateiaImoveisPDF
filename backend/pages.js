const MODELOS = new Set(['editorial', 'clean', 'bold', 'minimal']);

function esc(s) {
  return s == null ? '' : String(s)
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;');
}

function perfil(raw = {}) {
  return {
    nome: String(raw.nome || '').trim(),
    marca: String(raw.marca || '').trim(),
    creci: String(raw.creci || '').trim(),
    whatsapp: String(raw.whatsapp || '').trim(),
    instagram: String(raw.instagram || '').trim(),
    email: String(raw.email || '').trim(),
    foto: /^https?:\/\//i.test(raw.foto || '') ? raw.foto : '',
    logo: /^https?:\/\//i.test(raw.logo || '') ? raw.logo : '',
    corPrincipal: /^#[0-9a-f]{6}$/i.test(raw.corPrincipal || '') ? raw.corPrincipal : '#1f2e3f',
    corSecundaria: /^#[0-9a-f]{6}$/i.test(raw.corSecundaria || '') ? raw.corSecundaria : '#c25b3a',
    usarCores: !!raw.usarCores,
  };
}

const PALETAS = {
  editorial: { primary:'#233126', accent:'#8c603e', bg:'#e8e1d7', paper:'#f6f0e7', soft:'#6a7657', contrast:'#4a1a23' },
  clean: { primary:'#1f2e3f', accent:'#c25b3a', bg:'#e8e3da', paper:'#fffdf9', soft:'#7a846a', contrast:'#1f2e3f' },
  bold: { primary:'#2b1f17', accent:'#f27a34', bg:'#f2e5d8', paper:'#faf4ec', soft:'#d4a72c', contrast:'#214034' },
  minimal: { primary:'#6f7a61', accent:'#c46b57', bg:'#eee8e2', paper:'#fbf7f2', soft:'#a7b38a', contrast:'#6f7a61' },
};

export function errorPage(msg) {
  return `<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Busca Certa</title><style>*{box-sizing:border-box}body{margin:0;background:#ece7df;font-family:Inter,system-ui,sans-serif;display:grid;place-items:center;min-height:100vh;color:#26313a}.box{background:#fffdf9;padding:32px;border-radius:20px;max-width:520px;box-shadow:0 24px 70px rgba(31,46,63,.12)}.brand{font-family:Georgia,serif;color:#1f2e3f;font-size:25px}.box h2{font-family:Georgia,serif;color:#1f2e3f;font-weight:500;font-size:30px;margin:20px 0 8px}.box p{color:#788087;line-height:1.6}</style></head><body><div class="box"><div class="brand">Busca Certa</div><h2>Não encontramos esta seleção.</h2><p>${esc(msg)}</p></div></body></html>`;
}

export function clientPage(entrada) {
  const p = perfil(entrada.perfil || {});
  const modelo = MODELOS.has(entrada.modelo) ? entrada.modelo : 'editorial';
  const pal = PALETAS[modelo];
  const primary = p.usarCores ? p.corPrincipal : pal.primary;
  const accent = p.usarCores ? p.corSecundaria : pal.accent;
  const imoveisJson = JSON.stringify(entrada.imoveis || []).replace(/<\/script>/gi, '<\\/script>');
  const votosJson = JSON.stringify(entrada.votos || {}).replace(/<\/script>/gi, '<\\/script>');
  const perfilJson = JSON.stringify(p).replace(/<\/script>/gi, '<\\/script>');
  const contato = [p.creci, p.whatsapp, p.instagram, p.email].filter(Boolean).join(' · ');
  const nome = entrada.cliente ? esc(entrada.cliente) : '';

  return `<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="${primary}"><title>${nome ? `Seleção para ${nome}` : 'Seleção de imóveis'} | Busca Certa</title><style>
:root{--v:${primary};--a:${accent};--bg:${pal.bg};--paper:${pal.paper};--soft:${pal.soft};--contrast:${pal.contrast};--text:#26313a;--muted:#737c83;--border:#d8d2c8}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 5% 0%,color-mix(in srgb,var(--a) 10%,transparent),transparent 28rem),var(--bg);font-family:Inter,system-ui,-apple-system,sans-serif;color:var(--text)}.wrap{width:min(1020px,calc(100% - 28px));margin:26px auto 96px}.topo{position:relative;overflow:hidden;background:var(--v);color:#fff;padding:34px;border-radius:22px;box-shadow:0 18px 50px rgba(31,46,63,.16)}.topo:after{content:"";position:absolute;right:-70px;top:-90px;width:250px;height:250px;border:1px solid rgba(255,255,255,.18);border-radius:47% 53% 52% 48%;transform:rotate(24deg)}.brand{display:flex;align-items:center;gap:9px;font-family:Georgia,serif;font-size:17px;margin-bottom:30px}.brandmark{width:31px;height:31px;border:1px solid rgba(255,255,255,.45);border-radius:11px 11px 11px 3px;display:grid;place-items:center;font-size:11px;color:color-mix(in srgb,var(--a) 75%,#fff)}.ey{font-size:9px;color:color-mix(in srgb,var(--a) 75%,#fff);font-weight:900;letter-spacing:.15em;text-transform:uppercase}.topo h1{font-family:Georgia,serif;font-size:clamp(30px,5vw,50px);line-height:1.02;letter-spacing:-.03em;font-weight:500;margin:7px 0 10px;max-width:760px}.intro{max-width:690px;font-size:14px;line-height:1.55;color:rgba(255,255,255,.7);margin:0 0 22px}.perfil{display:flex;gap:11px;align-items:center;font-size:12px;color:rgba(255,255,255,.75)}.perfil img{width:44px;height:44px;border-radius:50%;object-fit:cover;border:1px solid rgba(255,255,255,.28)}.perfil strong{display:block;color:#fff}.perfil span{display:block;font-size:10px;margin-top:3px}.card{background:var(--paper);margin:17px 0;border-radius:18px;overflow:hidden;box-shadow:0 10px 34px rgba(31,46,63,.08);border:1px solid rgba(216,210,200,.8)}.head{padding:24px 25px 20px;border-bottom:1px solid var(--border)}.head small{color:var(--a);font-weight:900;text-transform:uppercase;letter-spacing:.12em;font-size:9px}.head h2{font-family:Georgia,serif;color:var(--v);font-weight:500;letter-spacing:-.02em;margin:5px 0;font-size:29px}.head p{margin:0;color:var(--muted);font-size:11px}.tipos{padding:16px 24px;overflow:auto}.tipos table{width:100%;border-collapse:collapse;min-width:520px}.tipos th{font-size:8px;text-transform:uppercase;letter-spacing:.08em;color:var(--muted);padding:7px 5px;border-bottom:1px solid var(--border)}.tipos td{font-size:12px;padding:10px 5px;border-bottom:1px solid var(--border);text-align:center}.tipos th:first-child,.tipos td:first-child{text-align:left;font-weight:800}.heroes{display:grid;grid-template-columns:1fr 1fr;gap:4px;padding:0 4px}.heroes img{width:100%;height:345px;object-fit:cover}.gal{display:grid;grid-template-columns:repeat(3,1fr);gap:4px;margin-top:4px;padding:0 4px}.gal img{width:100%;height:220px;object-fit:cover}.plants,.details{padding:20px 24px;border-top:1px solid var(--border)}.plants h3{font-size:9px;text-transform:uppercase;letter-spacing:.12em;color:var(--muted)}.plants-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:12px}.plant{background:#fff;padding:9px;border-radius:10px;border:1px solid var(--border)}.plant img{width:100%;height:300px;object-fit:contain}.plant span{font-size:10px;color:var(--muted)}.desc{font-size:13px;line-height:1.7;color:#485057}.tags{display:flex;gap:6px;flex-wrap:wrap;margin-top:13px}.tag{border:1px solid var(--border);border-radius:999px;font-size:8px;text-transform:uppercase;color:var(--muted);padding:5px 8px}.foot{padding:13px 24px;background:color-mix(in srgb,var(--v) 95%,#fff);color:#fff;display:flex;justify-content:space-between;font-size:10px}.foot a{color:inherit}.rate{padding:16px 24px;display:flex;align-items:center;gap:8px;background:color-mix(in srgb,var(--paper) 78%,var(--bg))}.rate span{margin-right:auto;font-size:9px;text-transform:uppercase;letter-spacing:.09em;color:var(--muted);font-weight:800}.vote{border:1px solid var(--border);background:#fff;padding:10px 13px;border-radius:10px;font-weight:800;color:var(--v)}.vote.like.ativo{background:var(--v);border-color:var(--v);color:#fff}.vote.no.ativo{background:#8d4a44;border-color:#8d4a44;color:#fff}.send{position:fixed;bottom:0;left:0;right:0;background:rgba(31,46,63,.95);display:flex;justify-content:center;padding:12px;z-index:20;backdrop-filter:blur(10px)}.send button{background:var(--a);color:#fff;border:0;border-radius:12px;padding:12px 24px;font-weight:900}.done{background:var(--paper);padding:60px 24px;text-align:center;border-radius:18px}.done h2{font-family:Georgia,serif;color:var(--v);font-size:34px;font-weight:500}.done p{color:var(--muted)}
body.modelo-editorial .topo{background:linear-gradient(115deg,#233126,#18231c)}body.modelo-editorial .head{border-top:5px solid #4a1a23}body.modelo-clean .topo{background:linear-gradient(115deg,#1f2e3f,#2b4358)}body.modelo-clean .card{background:#fff}body.modelo-bold .topo{background:linear-gradient(115deg,#2b1f17,#493020)}body.modelo-bold .head h2{font-size:32px}body.modelo-bold .card{border-radius:8px}body.modelo-minimal .topo{background:linear-gradient(115deg,#6f7a61,#58624e)}body.modelo-minimal .card{box-shadow:none}body.modelo-minimal .head h2{font-weight:400}@media(max-width:680px){.wrap{width:100%;margin-top:0}.topo,.card{border-radius:0}.topo{padding:26px 19px}.heroes img{height:44vw}.gal{grid-template-columns:repeat(2,1fr)}.gal img{height:48vw}.plants-grid{grid-template-columns:1fr}.plant img{height:auto}.rate{position:sticky;bottom:57px;flex-wrap:wrap}.rate span{width:100%;margin:0 0 3px}.vote{flex:1}.foot{flex-direction:column;gap:5px}.head,.tipos,.plants,.details,.rate,.foot{padding-left:18px;padding-right:18px}}
</style></head><body class="modelo-${modelo}"><main class="wrap"><header class="topo"><div class="brand"><span class="brandmark">BC</span>Busca Certa</div><div class="ey">Seleção personalizada pelo seu corretor</div><h1>${entrada.cliente ? `${esc(entrada.cliente)}, estas opções foram escolhidas para a sua busca.` : 'Uma seleção pensada para a sua busca.'}</h1><p class="intro">Veja os imóveis com calma e marque o que combina com o que você procura. Seu retorno ajuda o corretor a deixar as próximas opções ainda mais alinhadas.</p><div class="perfil">${p.foto ? `<img src="${esc(p.foto)}" alt="Foto do corretor">` : ''}<div><strong>${esc(p.nome || p.marca || 'Seu corretor')}</strong><span>${esc(p.marca || '')}${contato ? ` · ${esc(contato)}` : ''}</span></div></div></header><div id="lista"></div></main>${entrada.votos ? '<div class="send"><button disabled>✓ Avaliação enviada</button></div>' : '<div class="send" id="barra"><button id="enviar" onclick="sendVotes()">Enviar minhas escolhas</button></div>'}<script>
const ID=${JSON.stringify(entrada.id)},ITENS=${imoveisJson},SALVOS=${votosJson},JA=${!!entrada.votos},PERFIL=${perfilJson},V={};
const e=s=>s==null?'':String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
function table(d){const ts=d.tipologias||[];if(!ts.length)return'';const cs=[['preco_venda','A partir de','preco_aluguel'],['area_util','m²','area_total'],['quartos','Dorms'],['suites','Suítes'],['vagas','Vagas']].filter(c=>ts.some(t=>t[c[0]]!=null||(c[2]&&t[c[2]]!=null)));return '<div class="tipos"><table><thead><tr>'+cs.map(c=>'<th>'+c[1]+'</th>').join('')+'</tr></thead><tbody>'+ts.map(t=>'<tr>'+cs.map(c=>'<td>'+e(t[c[0]]??(c[2]?t[c[2]]:'—')??'—')+'</td>').join('')+'</tr>').join('')+'</tbody></table></div>'}
function render(){document.getElementById('lista').innerHTML=ITENS.map((it,i)=>{if(!it.ok)return'';const d=it.dados,f=(d.fotos||[]).filter(Boolean),h=(d._heroes||f.slice(0,2)).filter(u=>f.includes(u)).slice(0,2);while(h.length<Math.min(2,f.length)){const x=f.find(u=>!h.includes(u));if(!x)break;h.push(x)}const rest=f.filter(x=>!h.includes(x)),pl=(d.plantas||[]).map(x=>typeof x==='string'?{url:x,descricao:'Planta'}:x).filter(x=>x?.url),hero=h.length?'<div class="heroes">'+h.map(x=>'<img src="'+e(x)+'" alt="Imagem do imóvel">').join('')+'</div>':'',gal=rest.length?'<div class="gal">'+rest.map(x=>'<img loading="lazy" src="'+e(x)+'" alt="Imagem do imóvel">').join('')+'</div>':'',plants=pl.length?'<section class="plants"><h3>Plantas disponíveis</h3><div class="plants-grid">'+pl.map(x=>'<div class="plant"><img loading="lazy" src="'+e(x.url)+'" alt="Planta do imóvel"><span>'+e(x.descricao||'Planta')+'</span></div>').join('')+'</div></section>':'',tags=(d.caracteristicas||[]).slice(0,12).map(x=>'<span class="tag">'+e(x)+'</span>').join(''),saved=SALVOS[i]||'';return '<article class="card"><div class="head"><small>'+e([d.bairro,d.cidade].filter(Boolean).join(' · '))+'</small><h2>'+e(d.titulo||'Imóvel')+'</h2><p>'+e([d.codigo,d.endereco].filter(Boolean).join(' · '))+'</p></div>'+table(d)+hero+gal+plants+'<section class="details">'+(d.descricao?'<p class="desc">'+e(d.descricao)+'</p>':'')+(tags?'<div class="tags">'+tags+'</div>':'')+'</section><div class="foot"><strong>'+e(PERFIL.marca||PERFIL.nome||'Seu corretor')+'</strong>'+(d.url_origem?'<a target="_blank" rel="noreferrer" href="'+e(d.url_origem)+'">Ver anúncio original ↗</a>':'')+'</div><div class="rate"><span>Esta opção faz sentido?</span><button '+(JA?'disabled':'')+' class="vote like '+(saved==='like'?'ativo':'')+'" onclick="vote('+i+',\'like\',this)">♡ Gostei</button><button '+(JA?'disabled':'')+' class="vote no '+(saved==='dislike'?'ativo':'')+'" onclick="vote('+i+',\'dislike\',this)">Não é pra mim</button></div></article>'}).join('')}
function vote(i,t,b){if(JA)return;const r=b.closest('.rate');if(V[i]===t){delete V[i];r.querySelectorAll('.vote').forEach(x=>x.classList.remove('ativo'));return}V[i]=t;r.querySelectorAll('.vote').forEach(x=>x.classList.remove('ativo'));b.classList.add('ativo')}
async function sendVotes(){const b=document.getElementById('enviar');b.disabled=true;b.textContent='Enviando…';try{const r=await fetch('/api/votar/'+ID,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({votos:V})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.erro||'Não consegui enviar.');document.getElementById('lista').innerHTML='<div class="done"><h2>Escolhas enviadas ✓</h2><p>Seu corretor já pode ver o que fez mais sentido para você.</p></div>';document.getElementById('barra').remove()}catch(err){b.disabled=false;b.textContent='Enviar minhas escolhas';alert(err.message||'Não consegui enviar. Tente novamente.')}}render();
</script></body></html>`;
}

export function resultPage(entrada) {
  const p = perfil(entrada.perfil || {});
  const votes = entrada.votos || {};
  const ok = (entrada.imoveis || []).map((i, idx) => i?.ok ? { ...i.dados, idx } : null).filter(Boolean);
  const sections = [
    ['Gostou', ok.filter(d => votes[d.idx] === 'like'), '#1f2e3f'],
    ['Não é pra mim', ok.filter(d => votes[d.idx] === 'dislike'), '#8d4a44'],
    ['Sem avaliação', ok.filter(d => !votes[d.idx]), '#7a846a'],
  ];
  const body = sections.map(([title, list, color]) => `<section><h2 style="color:${color}">${title} (${list.length})</h2>${list.length ? list.map(d => {
    const photo = (d.fotos || []).find(Boolean);
    const price = d.preco_venda || d.preco_aluguel || (d.tipologias || []).map(t => t.preco_venda || t.preco_aluguel).find(Boolean) || '';
    return `<div class="item">${photo ? `<img src="${esc(photo)}" alt="Imagem do imóvel">` : ''}<div><small>${esc(d.codigo || '')}</small><strong>${esc(d.titulo || 'Imóvel')}</strong><span>${esc(price)}</span>${d.url_origem ? `<a href="${esc(d.url_origem)}" target="_blank">Ver anúncio ↗</a>` : ''}</div></div>`;
  }).join('') : '<p class="none">Nenhum imóvel aqui.</p>'}</section>`).join('');

  return `<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Resultado | Busca Certa</title><style>*{box-sizing:border-box}body{margin:0;background:#ece7df;font-family:Inter,system-ui,sans-serif;color:#26313a}.wrap{width:min(760px,calc(100% - 24px));margin:24px auto}.head{background:#1f2e3f;color:#fff;padding:27px;border-radius:18px 18px 0 0}.brand{font-family:Georgia,serif;font-size:16px;color:#e6dfd0;margin-bottom:16px}.head h1{font-family:Georgia,serif;font-weight:500;font-size:34px;letter-spacing:-.02em;margin:0}.head p{opacity:.68;font-size:12px}.body{background:#fffdf9;padding:24px;border-radius:0 0 18px 18px;box-shadow:0 20px 60px rgba(31,46,63,.1)}.body>a{color:#c25b3a;font-weight:800;font-size:12px}section{margin-top:25px}section h2{font-size:10px;text-transform:uppercase;letter-spacing:.11em;border-bottom:2px solid currentColor;padding-bottom:7px}.item{display:flex;gap:12px;background:#f7f3ec;border:1px solid #ddd6ca;padding:10px;margin:8px 0;border-radius:12px}.item img{width:82px;height:82px;object-fit:cover;border-radius:8px}.item div{display:flex;flex-direction:column;gap:3px}.item small{color:#c25b3a;font-weight:800}.item strong{color:#1f2e3f}.item span{font-size:12px;color:#788087}.item a{font-size:11px;color:#1f2e3f}.none{color:#888;font-size:12px}</style></head><body><main class="wrap"><header class="head"><div class="brand">Busca Certa</div><h1>${esc(entrada.cliente ? `Retorno de ${entrada.cliente}` : 'Retorno da seleção')}</h1><p>${esc(p.nome || p.marca || 'Corretor')}${p.marca ? ` · ${esc(p.marca)}` : ''}</p></header><div class="body"><a href="/ver/${esc(entrada.id)}" target="_blank">Abrir como o cliente vê ↗</a>${body}</div></main></body></html>`;
}
